import React, { Component } from 'react';

class PrimaryButton extends Component {

  render() {
    return (
        <button>
          Primary Button
        </button>
    );
  }
}

export default PrimaryButton;
